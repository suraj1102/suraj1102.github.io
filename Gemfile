source "https://rubygems.org"

gem "jekyll", "~> 3.8.5"

gem "webrick", "~> 1.8"

# Gems that left Ruby's default set in 3.3/3.4 but are still needed by Jekyll
# 3.x and its dependencies (kramdown needs rexml).
gem "rexml"
gem "csv"
gem "base64"
gem "bigdecimal"
